<%= form_with(model: task, local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :session_id %>
    <%= form.text_field :session_id, id: :task_session_id %>
  </div>

  <div class="field">
    <%= form.label :item_id %>
    <%= form.text_field :item_id, id: :task_item_id %>
  </div>

  <div class="field">
    <%= form.label :task_status %>
    <%= form.text_field :task_status, id: :task_task_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
