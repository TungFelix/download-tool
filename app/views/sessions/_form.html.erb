<%= form_with(model: session, local: true) do |form| %>
  <% if session.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(session.errors.count, "error") %> prohibited this session from being saved:</h2>

      <ul>
      <% session.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :source_id %>
    <%= form.text_field :source_id, id: :session_source_id %>
  </div>

  <div class="field">
    <%= form.label :session_status %>
    <%= form.text_field :session_status, id: :session_session_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
